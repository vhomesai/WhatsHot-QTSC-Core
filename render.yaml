services:
  - type: web
    name: whatshot-qtsc-core
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      - key: WHOT_ENTERPRISE_API_KEYS
        sync: false
